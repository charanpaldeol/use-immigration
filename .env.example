# Public site URL — drives canonical URLs, sitemap.xml, robots.txt host, and JSON-LD.
# Use your production origin (must match Google Search Console / Bing property).
NEXT_PUBLIC_SITE_URL=https://usdimmigration.ca
